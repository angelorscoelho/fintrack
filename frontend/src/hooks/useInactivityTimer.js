// Inactivity timer hook — Implemented in Session S10E
import { useEffect } from 'react'

export function useInactivityTimer(timeoutMs, onIdle) {
  useEffect(() => {
    // Implemented in S10E
  }, [])
}
