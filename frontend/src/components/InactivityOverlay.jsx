// Inactivity overlay dialog — Implemented in Session S10E
export function InactivityOverlay({ isVisible, onResume }) {
  if (!isVisible) return null
  return null // placeholder — full shadcn Dialog implementation in S10E
}
